# Contributing

- Keep changes small and test-backed.
- Run `ruff check .` and `pytest` before opening a PR.
- Update docs when behavior changes.
- Prefer services and serializers over fat views.
- Never add TODO-only placeholders in production paths.
